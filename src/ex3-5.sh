#!/bin/bash

# 내부 함수 정의: ls 명령어를 실행하되, 전달된 옵션을 실행
run_ls() {
	# "$@" 에 들어온 옵션들을 그대로 붙여서 ls에 전달
	cmd="ls $*"
	echo "실행할 명령 : $cmd"
	eval "$cmd"
}

echo "== ex3-5: ls 명령어 실행 함수 =="

# 스크립트에 넘긴 인자를 그대로 함수로 전달
# 예: ./ex3-5.sh -l -a
run_ls "$@"
